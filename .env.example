# ───────────────────────────────
# 🧠 LLM Keys
# ───────────────────────────────
OPENAI_API_KEY=open-ai-api-key
CLAUDE_API_KEY=your-claude-key
CLAUDE_API_ENDPOINT=https://api.anthropic.com/v1/messages

# ───────────────────────────────
# 🪙 Zerodha Keys (for LIVE mode)
# ───────────────────────────────
KITE_API_KEY=kite_api_key_here
KITE_ACCESS_TOKEN=kite_access_token_here

# ───────────────────────────────
# ⚙️  Misc / Logging
# ───────────────────────────────
TRADER_LOG_DIR=logs
TRADER_LOG_RETENTION_DAYS=7

# ───────────────────────────────
# 📊 Application Logging & Tracing
# ───────────────────────────────
# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO

# Log format: json or text (default: json)
# json = structured JSON logs (recommended for production)
# text = human-readable text logs (good for development)
LOG_FORMAT=json

# Enable detailed logging (default: false)
# When true: includes source file/line numbers, debug messages, operation timings
# When false: only INFO/WARN/ERROR messages, no performance overhead
LOG_DETAILED=false

# Enable request tracing (default: true)
# When true: adds trace_id to all log entries for tracking request flow
# When false: no trace IDs, slightly better performance
LOG_TRACING_ENABLED=true

# ───────────────────────────────
# 📈 PEAD Research Module
# ───────────────────────────────
# Minimum PEAD composite score (0-100) to qualify for trading
# Companies with this score or above will be considered
# Examples: 40 = moderate quality, 60 = high quality, 80 = exceptional
PEAD_MIN_SCORE=40

# Optional: API key for earnings data provider
# Leave empty to use mock data for testing
EARNINGS_API_KEY=
